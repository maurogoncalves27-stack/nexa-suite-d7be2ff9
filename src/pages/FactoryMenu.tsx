import { Navigate } from "react-router-dom";

// Consolidado em /produtos-fabrica?view=cardapio (mesma base de cadastro)
const FactoryMenu = () => <Navigate to="/produtos-fabrica?view=cardapio" replace />;

export default FactoryMenu;
